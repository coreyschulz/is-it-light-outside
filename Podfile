# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Is it Light Outside?' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Is it Light Outside?
  
  target 'Is it Light Outside?Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Is it Light Outside?UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
